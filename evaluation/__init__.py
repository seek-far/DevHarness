"""
Evaluation mode — sweep agents × fixtures, compare approaches.

Not to be confused with running mode (bf_worker/standalone.py, bf_worker/bf_worker.py)
which uses ONE agent against a real bug. Evaluation mode runs MANY agents against
a curated benchmark and produces a comparison report.

See: README.md for architecture overview.
"""
