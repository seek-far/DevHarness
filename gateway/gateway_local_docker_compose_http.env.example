# Stage-1 cloud rehearsal — gateway. Containerized; reaches the compose Redis
# by service name. gateway_stream must match the orchestrator's.
USE_REDIS=True
REDIS_URL=redis://redis:6379/15
GATEWAY_STREAM=gateway:stream
