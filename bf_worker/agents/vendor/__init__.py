"""Third-party source vendored into this repository.

Everything under this package is copied from an upstream project and kept
under that project's own license. The directory boundary IS the attribution
boundary: if a file lives here it is NOT SDLCMA-original code, it has a
LICENSE next to it, and its provenance is recorded in the sibling
``UPSTREAM.md``.

Do not put SDLCMA-original code here, and do not run formatters over it —
see ``mini/UPSTREAM.md`` for why (short version: a parity test compares these
files byte-for-byte against the installed upstream package).
"""
